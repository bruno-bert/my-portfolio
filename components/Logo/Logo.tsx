import styles from './Logo.module.scss'

interface Props {

}

const Logo = (props: Props) => {
    return (
        <div className={styles.logoWrapper}>
            B
        </div>

    )
}

export default Logo